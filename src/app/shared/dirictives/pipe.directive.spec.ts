import { PipeDirective } from './pipe.directive';

describe('PipeDirective', () => {
  it('should create an instance', () => {
    const directive = new PipeDirective();
    expect(directive).toBeTruthy();
  });
});
